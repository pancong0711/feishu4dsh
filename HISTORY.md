# 开发历史（HISTORY）

> 本文档面向仓库访客：说明这个项目是如何开发的、版本如何演进、以及为什么 git 历史呈现为「快照式」。逐条变更明细见 [CHANGELOG.md](CHANGELOG.md)。

## 一、这个项目是怎么开发的

本项目的全部代码与文档由 **DeepSeek Harness（dsh）Agent 会话协作**完成，人类所有者负责提需求、做决策与最终把关：

1. **需求先落文档**：每个可交付需求写成工作单（目标 / 改动边界 / 验收标准），无单不派工；
2. **后台子任务实施**：主会话把工作单派给后台 subagent 执行，会话保持畅通；
3. **评审与门禁**：主会话逐行评审 diff，并独立复核三道门禁——严格类型检查（`noUncheckedIndexedAccess`）、全量单测（vitest，离线不触网）、构建产物；
4. **回归纪律**：修任何 bug 必须同时固化一条覆盖该 bug 的回归测试；通道行为用假端口（FakePort）离线验证。

工程数据的一个切面：首版约 80 项测试，一周内迭代到 **190 项**，几乎每个版本都伴随新增强化与回归。

## 二、版本里程碑

| 版本 | 日期 | 主题 |
|---|---|---|
| 0.1.0 | 2026-08-19 | 首个可用版本：WebSocket/Webhook 双传输、三种会话作用域（chat/chat-thread/chat-sender）、流式与聚合两种回复渲染 |
| 0.1.1 | 2026-08-19 | 更名 `feishu4dsh`；接入 CI |
| 0.2.0 | 2026-08-19 | **多工作区**：会话身份升级为 scope × workspace × generation，`/ws` `/cd` 手机端切换并持久化 |
| 0.2.1 | 2026-08-19 | `/help` 按来源分组标识（频道命令 / 宿主命令） |
| 0.3.0 | 2026-08-19 | 手机侧管理工作区（`/ws add/remove` + ACL 门控）；修复 Agent 默认模型注入 |
| 0.4.0 | 2026-08-25 | **切换链路加固**：默认工作区三级兜底、`/cd` 权限门控、并发创建去重、旧会话尾事件抑制；发布模型定稿 |
| 0.4.1 | 2026-08-25 | 私聊话题回复归属修复（入站批合并丢 thread_id）；中文路径兼容加固（全角空格 U+3000 规范化 + 配置自愈回写） |
| 0.4.2 | 2026-08-26 | `/new` 语义收敛（只开新会话，保留模型 pin）；审批卡片跟随来源话题；新会话默认完整工具集预设 |
| 0.4.3 | 2026-08-27 | 撤下 dsh-std 阶段 1 双轨产物（外部宿主启动失败修复），恢复单轨直至真实 adapter 验证 |
| 0.4.4 | 2026-08-27 | 用户消息资源改用 message-scoped API 下载（修复用户图片/文件 400） |
| 0.5.0 | 2026-08-28 | **体验对齐 dsh web**：`/status` 会话统计（模式 / 推理强度 / 轮次·步 / Token 累计）；修复 send_file 落聊天根；`/mode` 模式设置；`/model effort` 按模型推理强度 |
| 0.6.0 | 2026-08-28 | **会话管理（TUI 三件套）**：`/session` 列表 / 切回 / 重命名 / 归档（与 dsh web 共用归档集合）；修复重启后会话归零与"未分组"两项存量缺陷；启动版本日志 |
| 0.6.1 | 2026-09-02 | **修复带数据重启后全部会话操作抛错**：宿主深度冻结配置被按引用水合 → 水合边界重建注册表对象图 + agentKey 形状校验 + 冻结金丝雀 |

## 三、当前方向

- **后台任务工作流**：在飞书话题里派发后台子任务，汇报自动回到发起话题——"需求文档 → 后台执行 → 话题内验收"的闭环协作；
- **回复投递可靠性**：线程锚定与输出流活性持续加固，保证长任务场景下回复不丢、不串、不迟滞；
- **社区 dsh-std 互操作标准适配**（跟进中）：已完成兼容性调研；阶段试点因外部宿主启动问题曾撤下（见 CHANGELOG 0.4.3），待以真实 adapter 验证后恢复标准发布面；
- **体验持续对齐**：以 dsh web 为参照（`/status` 统计、`/mode`、`/model effort`），向 TUI 借鉴会话管理三件套（`/session` 列表 / 切回 / 重命名 / 归档），把两类界面的长处逐步带进飞书；后续路线：点击式会话切换卡片。
- **通道与 web 的会话互斥边界（长期）**：一个宿主会话只应有单一写入方，但宿主暂无归属声明/排他能力——web 误驱动 `feishu-*` 会话会串扰输出甚至损坏会话日志（v0.6.0 起通道会话在 web 可见后敞口变大）。当前以文档约定规避（README 安全边界 / 排障手册对应章节），根治待上游单写入方锁或插件侧回合发起方过滤。

## 四、发布模型：为什么 git 历史这么短

本仓库的公开形态是**刻意设计**的结果，采用「三层仓库」模型：

```
内部全量主仓（本地 main）
        │  白名单快照：源码 / 测试 / CI / 用户面文档
        ▼
去敏快照仓 ──完整测试通过后──> 公开远程 origin/main（force-push 单提交覆盖）
```

- 公开远程只接收**测试过的去敏快照**：内部过程文档、运行环境细节一律不出本地；
- 每次发布以**单提交 force-push** 覆盖远程 main——这是设计行为而非事故，历史不留内部内容；
- 每个 `published-snapshot-*` tag 对应一次完整验证过的发布。

因此：**追踪项目演进请以本文档 + [CHANGELOG.md](CHANGELOG.md) 为准，不要依赖 commit 历史。**

## License

[MIT](LICENSE)。非官方社区项目，与 DeepSeek、字节跳动、飞书、Lark 无从属/授权/背书关系。
